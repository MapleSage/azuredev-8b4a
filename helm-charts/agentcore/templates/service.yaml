apiVersion: v1
kind: Service
metadata:
  name: {{ include "sageinsure-agentcore.fullname" . }}
  labels:
    {{- include "sageinsure-agentcore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sageinsure-agentcore.selectorLabels" . | nindent 4 }}