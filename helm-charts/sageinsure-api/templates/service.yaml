apiVersion: v1
kind: Service
metadata:
  name: { { include "sageinsure-api.fullname" . } }
  labels: { { - include "sageinsure-api.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
      protocol: TCP
      name: http
  selector: { { - include "sageinsure-api.selectorLabels" . | nindent 4 } }
